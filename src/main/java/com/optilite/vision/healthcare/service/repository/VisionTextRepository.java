package com.optilite.vision.healthcare.service.repository;

import com.optilite.vision.healthcare.entity.VisionText;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VisionTextRepository extends JpaRepository<VisionText, Long> {
}
